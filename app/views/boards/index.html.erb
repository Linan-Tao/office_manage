<div class="page-title">
  <h2><i class="fa fa-desktop color"></i> 板件管理 <small>列表</small>
  <span class="pull-right">
    <%= link_to '添加', new_board_path, class: 'btn btn-lg btn-info' %>
  </span>
  </h2>
  <hr />
</div>
<div class="awidget full-width">
  <div class="awidget-body">
    <table class="table table-hover table-striped table-bordered table-condensed text-center">
      <thead>
        <tr class="info">
          <th class="text-center">编码</th>
          <th class="text-center">名称</th>
          <th class="text-center">状态</th>
          <th class="text-center" colspan="3">操作</th>
        </tr>
      </thead>
      <tbody>
        <% @boards.each do |board| %>
        <tr>
          <td><%= link_to board.code, board, class: 'detail-link' %></td>
          <td><%= board.name %></td>
          <td><%= board.status ? '可用' : '不可用' %></td>
          <td><%= link_to edit_board_path(board), class: 'btn btn-xs btn-warning' do %><i class="fa fa-pencil"></i><% end %>
            <%= link_to board, method: :delete, data: { confirm: '确定删除?' }, class: 'btn btn-xs btn-danger' do %><i class="fa fa-times"></i><% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<br>