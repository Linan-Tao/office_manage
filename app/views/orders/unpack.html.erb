<!-- Page title -->
<div class="page-title">
  <h2><i class="fa fa-desktop color"></i> 未包装订单管理 <small>列表</small>
    <span class="pull-right">
    </span>
  </h2>
  <hr />
</div>
<!-- Page title -->
<div class="awidget full-width">
  <table class="table table-hover table-striped table-bordered table-condensed text-center">
      <thead>
        <tr class="info">
          <td>包装</td>
          <td>客户单号</td>
          <td>接单时间</td>
          <td>订单状态</td>
          <td>经销商</td>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order|%>
        <tr>
          <td><%= link_to '开始包装', package_order_path(order), class: 'detail-link' %></td>
          <td><%= link_to order.name, order_path(order), class: 'detail-link' %></td>
          <td><%= order.created_at.localtime.strftime("%Y-%m-%d")%></td>
          <td><%= order.work.name%></td>
          <td><%= order.order_union.agent.try(:name) %></td>
        </tr>
        <%end%>
      </toby>
  </table>
</div>