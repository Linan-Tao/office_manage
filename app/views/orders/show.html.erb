<div class="page-title">
  <h2><i class="fa fa-desktop color"></i> 订单管理<small>查看订单</small>
    <span class="pull-right">
      <%= link_to '返回', orders_path, class: 'btn btn-lg btn-info' %>
      <%= link_to '编辑', edit_order_path(@order), class: 'btn btn-lg btn-info' %>
    </span>
  </h2>
  <hr />
</div>
<% if @order.not_separate?%>
   <div class="awidget full-width">
      <div class="awidget-head">
         <%=form_tag import_order_order_units_path(@order), multipart: true, class: 'form-inline' do%>
           <div class="row">
              <div class="col-md-6 col-sm-6">
                 <%= file_field_tag :file, required: true, accept: 'csv'%>
              </div>
              <div class="col-md-6 col-sm-6">
                 <%= submit_tag "导入拆单文件", class: 'btn btn-info btn-lg'%>
              </div>
           </div>
         <%end%>   
      </div>
      <div class="awidget-body">
         <p>请导入csv文件，表头为部件名称,板材,长,宽,厚,数量,裁切尺寸,柜体名称,订单号,订货商,终端信息,封边,纹理,备注,条码,流水号</p>
      </div>
   </div>
<% end %>

<div class="awidget full-width">
   <div class="awidget-head"> 
		<div class="row">
		   <div class="col-md-12 col-sm-12">
		      <h3>详细信息
               <% if @order.separated? %>
               <p class="pull-right">
                  <button type="button" class="btn btn-info" data-toggle="modal" data-target="#offer">
                    生成报价单
                  </button>
               </p>
               <% end %>
            </h3>
		   </div>
		</div>
   </div>
   <div class="awidget-body">
   	  <p>订单号：<%= @order.order_code%></p>
   	  <p>订单状态：<%= @order.work.name%></p>
   </div>
</div>
<%= render 'order_units'%>
<%= render 'order_parts'%>
<%= render 'order_offers'%>