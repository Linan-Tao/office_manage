<%= simple_form_for(@part_category, url: @part_category.new_record? ? part_categories_path : part_category_path(@part_category)) do |f| %>
  <%= f.input :name %>
  <%= f.input :note %>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-lg btn-block btn-primary'  %>
  </div>
<% end %>


